http {
    server {
        listen 80;
        location / {
            root /usr/share/nginx/html;
            index index.html pay-bill.html;
        }
    }
}
```

Note: This is a basic implementation and you should consider implementing proper security measures (e.g. validating user input, handling errors) in your actual application.